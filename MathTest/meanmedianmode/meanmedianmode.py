import statistics


numbers = [3,2,88,77, 77]

mean = statistics.mean(numbers)

median = statistics.median(numbers)

mode = statistics.mode(numbers)

print ("The mean is :", mean, "The median is :", median,"The mode is:", mode)

